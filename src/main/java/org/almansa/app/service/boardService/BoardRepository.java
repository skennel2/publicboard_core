package org.almansa.app.service.boardService;

import org.almansa.app.core.Repository;
import org.almansa.app.core.board.Board;

public interface BoardRepository extends Repository<Long, Board> {
	
}
