<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="MEMBER_MAPPER">
    
    <resultMap type="org.almansa.app.core.member.SimpleMember" id="memberMapper">
        <constructor>
            <idArg javaType="Long" column="ID"/>
            <arg javaType="String" column="LOGIN_ID"/>
            <arg javaType="String" column="PASSWORD"/>
        </constructor>
    </resultMap>
    
    <select id="getAll" resultMap="memberMapper">
        SELECT ID,
               LOGIN_ID,
               PASSWORD,
               IS_ADMIN
          FROM MEMBER
    </select>
    
</mapper>